# Taking first number as input and converting it to an integer
a = int(input("Enter a number: "))

# Taking second number as input and converting it to an integer
b = int(input("Enter another number: "))

# Printing both numbers and their sum using an f-string
print(f"The numbers are {a} and {b}, and the sum is {a + b}")