# Check the type of variable assigned using input.

# Taking input from the user (note: input() always returns a string)
_UserInput = input("Enter the value to check the type: ")

# Printing the value and its data type
print(f"The input value is {_UserInput}, and the type is {type(_UserInput)}")