# Can we have a set with 18 (int) and '18' (str) as a value in it? 

# Creating a set with both an integer and a string value
my_set = {18, '18'}  # 18 is int, '18' is str

# Printing the set
print(my_set)  # Output will show both values since they are of different types
# Output: {18, '18'}
